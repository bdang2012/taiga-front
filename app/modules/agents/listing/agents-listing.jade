div.project-list-wrapper.centered

    div
        h1 Agents
            table
                tr(tg-repeat="user in vm.agents ")
                    td(style="text-transform: capitalize", width="90%") {{user.get('full_name')}} 
                    td(style="text-transform: capitalize; color:blue")
                        a(href="" ng-click="vm.openDeactivateAgentLightbox(user)") Deactivate
